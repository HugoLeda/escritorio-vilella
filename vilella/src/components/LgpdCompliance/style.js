import styled from "styled-components"

export const LgpdContainer = styled.div `

  .flex {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .icon {
    align-items: center;
  }

  p {
    margin: 0;
  }

`